apply plugin: 'cpp'

model {
  components {
    ctgpio (NativeLibrarySpec) {
    }
    main (NativeExecutableSpec) {
      sources {
        cpp.lib library: 'ctgpio', linkage: 'static'
      }
    }
  }
}
